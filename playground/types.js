/* eslint-disable*/
/* const name = 'UmicornXxXdu91Branlix3000';
const middle = "topher";
const last = `bos`;

const sentence = "she's so cool";

const song = `Oh
yeah

I like

Pizza`;

const hello = "hello my name is " + name + ". Nice to meet you";

let hello2 = 'hello my name is ';
hello2 = hello2 + name;
hello2 = hello2 + '. Nice to meet you';

const hello = `hello my name is ${name}. Nice to meet you. I am ${1 + 22} years old`;

const html = `
<div>
  <h2>${name}</2>
  <p>${hello}</p>
</div>
`;

console.log(html);

document.body.innerHTML = html;

*/
/*
const age = 12.9;
const name = 'umi';

/* const a = 10;
const b = 20;

const smarties = 20;
const kids = 3;
const EachKidGets = Math.floor(smarties / kids);
console.log(`Each kids gets ${EachKidGets}`); */

/* const person = {
  first: 'umi',
  last: 'bos',
  age: 100,
  skills: {
    piano: 72
  }
};
 */

/* const teller = {
  first: 'Raymond',
  last: 'Teller',
};

teller.first = 'Teller';
teller.last = null;
 */
